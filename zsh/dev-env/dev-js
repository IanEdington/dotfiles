path=(
        node_modules/.bin
        $path
    )

yllc() {
  find node_modules -type l -maxdepth 3 | grep -v .bin | cut -f2 -d/
}

yll() {
  linkDir=~/.config/yarn/link
  find $linkDir -maxdepth 2 -type l | xargs realpath -s --relative-to $linkDir
}
